basePath: /api/v1
host: localhost:8080
info:
  contact: {}
paths:
  /health:
    get:
      consumes:
      - application/json
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: Health check
      tags:
      - General
swagger: "2.0"
